#include <stdio.h>

int main()
{
    /**
     * 补码
     *  1 = 00000001
     * -1 = 0 - 1
     *    = 100000000 - 00000001  (二进制计算)
     *    = 11111111
     *    = 2 ** n - x
     *
     * 优先级
     * ()
     * ! + - ++ -- 单目运算
     * * / %
     * + -
     * < > <= >=
     * == !=
     * &&
     * ||
     * x < y ? a : b
     * = += -= *= /= %= 赋值运算最低
     * , 最低 (且取最右侧的值作为结果)
     */
}